<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <!-- NOT -windows: the compiler must reject any Win32 or WPF use inside the core -->
    <TargetFramework>net10.0</TargetFramework>
  </PropertyGroup>
  <ItemGroup>
    <AssemblyAttribute Include="System.Runtime.CompilerServices.InternalsVisibleToAttribute">
      <_Parameter1>CorePin.Tests</_Parameter1>
    </AssemblyAttribute>
  </ItemGroup>
</Project>
